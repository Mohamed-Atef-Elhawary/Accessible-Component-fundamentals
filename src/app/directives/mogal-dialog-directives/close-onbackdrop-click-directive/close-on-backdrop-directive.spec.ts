import { CloseOnBackdropDirective } from './close-on-backdrop-directive';

describe('CloseOnBackdropDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOnBackdropDirective();
    expect(directive).toBeTruthy();
  });
});
