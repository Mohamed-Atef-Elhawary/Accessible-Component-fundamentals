import { CloseOnEscapeDirective } from './close-on-escape-directive';

describe('CloseOnEscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOnEscapeDirective();
    expect(directive).toBeTruthy();
  });
});
